// mc-designer agent tool definitions
// Phase 3: expose generate_image and composite_canvas as agent-callable tools
// so the agent can trigger image generation autonomously during a session.
// For now, designer is CLI-only (see cli/commands.ts).
export const designerTools: unknown[] = [];
