{
  "name": "mc-email",
  "version": "0.2.0",
  "type": "module",
  "main": "index.ts",
  "description": "Email integration — read, triage, archive, send via himalaya CLI",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "better-sqlite3": "^12.8.0"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.13",
    "@vitest/ui": "^2.0.0",
    "openclaw": "file:../../../openclaw",
    "vitest": "^2.0.0"
  },
  "openclaw": {
    "extensions": [
      "./index.ts"
    ]
  }
}
