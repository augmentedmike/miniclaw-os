{
  "name": "mc-oauth-guard",
  "version": "0.1.0",
  "type": "module",
  "main": "index.ts",
  "description": "OAuth token refresh failure guard — detects retry storms, applies backoff, auto-disables failing profiles, and attempts keychain recovery",
  "scripts": {
    "test": "vitest run"
  },
  "devDependencies": {
    "openclaw": "npm:@miniclaw_official/openclaw@^2026.3.8",
    "vitest": "^4.1.0"
  },
  "openclaw": {
    "extensions": [
      "./index.ts"
    ]
  }
}
