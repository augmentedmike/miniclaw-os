{
  "name": "mc-voice",
  "version": "0.1.0",
  "type": "module",
  "description": "Human voice message store \u2014 schema and embedding pipeline",
  "scripts": {
    "migrate": "npx tsx src/migrate.ts",
    "build": "tsc"
  },
  "dependencies": {
    "better-sqlite3": "*"
  },
  "devDependencies": {
    "@types/better-sqlite3": "*",
    "@types/node": "*",
    "tsx": "*",
    "typescript": "*"
  },
  "openclaw": {
    "extensions": [
      "./index.ts"
    ]
  }
}
