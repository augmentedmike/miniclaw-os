{
  "id": "mc-reflection",
  "name": "Miniclaw Reflection",
  "description": "Nightly self-reflection — reviews the day's memories, board state, KB entries, and session transcripts to extract lessons, create action items, and build long-term knowledge.",
  "version": "0.1.0",
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "reflectionDir": {
        "type": "string",
        "description": "Directory for reflection snapshots (default: ~/am/USER/augmentedmike_bot/reflections)"
      },
      "memoryDir": {
        "type": "string",
        "description": "Episodic memory directory (default: ~/am/workspace/memory)"
      },
      "boardDbPath": {
        "type": "string",
        "description": "Path to board.db (default: ~/am/USER/augmentedmike_bot/brain)"
      },
      "kbDbPath": {
        "type": "string",
        "description": "Path to kb directory (default: ~/am/USER/augmentedmike_bot/kb)"
      },
      "transcriptsDir": {
        "type": "string",
        "description": "Claude session transcripts directory (default: ~/.claude/projects)"
      }
    }
  }
}
