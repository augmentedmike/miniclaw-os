// mc-context agent tool definitions
// mc-context operates automatically via hooks — no agent-callable tools yet.
// Future: expose context window inspection as a tool so the agent can
// query its own retention state.
export const contextTools: unknown[] = [];
