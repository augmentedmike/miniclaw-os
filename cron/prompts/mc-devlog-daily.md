Daily devlog generator — runs at 8am CT.

1. Run the devlog pipeline: openclaw mc-devlog run
2. If successful, silent exit.
3. If any publisher fails, log the error but do not retry.
4. Never ask questions. Never generate conversational responses.
