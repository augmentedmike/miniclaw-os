{
  "id": "mc-github",
  "name": "MiniClaw GitHub",
  "description": "Manage GitHub issues, PRs, releases, and Actions workflows via gh CLI.",
  "version": "0.1.0",
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "defaultRepo": {
        "type": "string",
        "description": "Default GitHub repo (owner/name). If omitted, detected from git remote."
      }
    }
  }
}
