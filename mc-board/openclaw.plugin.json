{
  "id": "mc-board",
  "name": "Miniclaw Board",
  "description": "State-machine-backed kanban board as the agent's internal planning mechanism — the prefrontal cortex.",
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "cardsDir":       { "type": "string" },
      "qmdBin":         { "type": "string" },
      "qmdCollection":  { "type": "string" },
      "webPort":        { "type": "number" },
      "boardWebUrl":    { "type": "string", "description": "Base URL of the board web UI (e.g. Tailscale funnel URL). When set, card IDs in the board context are rendered as clickable links." }
    }
  }
}
