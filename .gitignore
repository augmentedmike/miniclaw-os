
.DS_Store
.env
.env.*
.next/
.plugins-prebuilt/
.secret
*.age
*.js.map
*.key
*.log
*.p12
*.pem
*.pfx
**/node_modules/
# Age-encrypted secrets (committed separately if needed, never plaintext)
# Build output
# End-user data — created and owned by the end user, never committed
# Environment and credential files
# Headless install config (contains real credentials)
# Logs
# Node dependencies
# OS noise
# Vault — NEVER commit secrets, keys, or encrypted blobs
credentials.json
dist/
examples/headless-config.json
node_modules/
secrets/
system/vault/
test-results/
Thumbs.db
TODO-*.md
user/
